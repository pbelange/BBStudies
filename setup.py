from setuptools import setup, find_packages

setup(
    name    ='BBStudies',
    version ='1.00',
    packages=find_packages(where='BBStudies'),  
    install_requires=[
        # List your dependencies here
    ],

)